{
  "name": "@res/server",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf ./dist && rm -rf ./node_modules",
    "prebuild": "rm -rf ./dist",
    "--------- BUILD ---------": "--------- BUILD ---------",
    "build": "tsc --build",
    "--------- WATCH ---------": "--------- WATCH ---------",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@citizenfx/server": "^2.0.9073-1",
    "@types/node": "^20.14.12",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=16",
    "pnpm": ">=7"
  }
}
